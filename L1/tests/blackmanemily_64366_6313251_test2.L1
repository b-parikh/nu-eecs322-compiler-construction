(:main
	(:main
	0 0
	rdi <- 7 //3
	rsi <- 13 //6
	mem rsp -8 <- :unfun
	call :funmath 2
	:unfun
	rdi <- rax
	call print 1

	rdi <- 5 //2
	rsi <- 105 //52
	mem rsp -8 <- :unfunner
	call :funmath 2
	:unfunner
	rdi <- rax
	call print 1

	return)

	// x = x+y
	// y = y+x
	// return y	
	(:funmath
	2 1
	rdi >>= 1
	rsi >>= 1
	rdi += rsi
	rsi += rdi
	rsi <<= 1
	rsi += 1
	rdi <- rsi
	rax <- rsi
	mem rsp 0 <- rdi
	call print 1  //should print 3 + 6 + (3+6) = 18
	rdi <- mem rsp 0
	rax <- rdi
	return
	)
)
//output should be:
// 15
// 15
// 106
// 106