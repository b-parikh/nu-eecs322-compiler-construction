(:go
 (:go
  0 0
  rdi <- 10
  rsi <- 9
  mem rsp -8 <- :max_ret
  call :max 2
  :max_ret
  rax <<= 1
  rax++
  rdi <- rax
  call print 1
  rdi <- 10
  rsi <- 3
  mem rsp -8 <- :div_ret
  call :divide 2
  :div_ret
  rax <<= 1
  rax++
  rdi <- rax
  call print 1
  return
  )

 (:max
  2 0
  %first <- rdi
  %second <- rsi
  cjump %second < %first :FIRST
  rax <- %second
  return
  :FIRST
  rax <- %first
  return
 )

 (:divide
  2 0
  %quotient <- 0
  :START
  rdi -= rsi
  cjump rdi <= 0 :DONE
  %quotient++
  goto :START
  :DONE
  rax <- %quotient
  return
 )
)
